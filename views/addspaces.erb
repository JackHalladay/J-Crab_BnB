<!DOCTYPE html>
<html lang="en-US">
<style>
form {
  /* Center the form on the page */
  margin: 0 auto;
  width: 500 px;
  /* Form outline */
  padding: 1em;
  border: 8px solid #CCC;
  border-radius: .75em;
}

ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

form li + li {
  margin-top: 1em;
}

label {
  /* Uniform size & alignment */
  display: inline-block;
  width: 90px;
  text-align:    center;
}

input,
textarea {
  /* To make sure that all text fields have the same font settings
     By default, textareas have a monospace font */
  font: 1em sans-serif;

  /* Uniform text field size */
  height: 100px;
  width: 600px;
  box-sizing: border-box;

  /* Match form field borders */
  border: 5px solid #999;
}

input:focus,
textarea:focus {
  /* Additional highlight for focused elements */
  border-color: 333;
}

textarea {
  /* Align multiline text fields with their labels */
  vertical-align: top;

  /* Provide space to type some text */
  height: 5em;
}

.button {
  /* Align buttons with the text fields */
  padding-left: 180px; /* same size as the label elements */
}

button {
  /* This extra margin represent roughly the same space as the space
     between the labels and their text fields */
  margin-left: .5em;
}
</style>
<body style="background-color:#cd7f84;" >
<h1>Add a Space</h1>

<form method="post" action="/spaces">
<input type="text" name="inputspacetextbox" placeholder="inputspacetextbox" size="200" />
<input type="text" name="inputdesctextbox" placeholder="inputdesctextbox" size="200" />
<input type="submit" value="Submit" />
</form>
</body>