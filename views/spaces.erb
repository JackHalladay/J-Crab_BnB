<!DOCTYPE html>
<html lang="en-US">
<style>
form {
  /* Center the form on the page */
  margin: 0 auto;
  width: 250code px;
  /* Form outline */
  padding: 1em;
  border: 8px solid #CCC;
  border-radius: .75em;
}

ul {
  list-style: none;
  padding: 0;
  margin: 0;
}

form li + li {
  margin-top: 1em;
}

label {
  /* Uniform size & alignment */
  display: inline-block;
  width: 90px;
  text-align:    center;
}

input,
textarea {
  /* To make sure that all text fields have the same font settings
     By default, textareas have a monospace font */
  font: 1em sans-serif;

  /* Uniform text field size */
  width: 300px;
  box-sizing: border-box;

  /* Match form field borders */
  border: 5px solid #999;
}

input:focus,
textarea:focus {
  /* Additional highlight for focused elements */
  border-color: 333;
}

textarea {
  /* Align multiline text fields with their labels */
  vertical-align: top;

  /* Provide space to type some text */
  height: 5em;
}

.button {
  /* Align buttons with the text fields */
  padding-left: 180px; /* same size as the label elements */
}

button {
  /* This extra margin represent roughly the same space as the space
     between the labels and their text fields */
  margin-left: .5em;
}
</style>

<h1>View our Spaces</h1>
<% if @user %>
<h1>Welcome, <%= @user.email %></h1>
<% end %>
<br><form method="get" action="/addspaces">
<input type="submit" value="Add a Space" />
</form>
<body style="background-color:#cd7f84;" >
</br>
<form action="/sessions/destroy" method="post">
  <input type="submit" value="Sign out" />
</form>
<h2>These are the current rooms</h2>


<div>
<% @spaces.each do |spaces| %>
  <table>
  <tr>
  <td style="text-align: bottom"> <form method="post" action="/requestspaces">
  <input type="hidden" name="id" value="<%= spaces.id %>">
  <input type="hidden" name="name" value="<%= spaces.name %>">
  <input type="hidden" name="description" value="<%= spaces.description %>">
  <input type="submit" value="Request to book a Space <%= spaces.id %>"  />
  </form> </td>
  <td style="text-align: center">  Name: <%= spaces.name  %> </td>
  <td style="text-align: center">  Description: <%= spaces.description  %> </td>
  </tr>
  <% end %>
</div>






